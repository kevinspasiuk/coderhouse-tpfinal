## Ejercicio Primera entrega

Este repositorio usa express para crear un servidor web que implementa los endpoint /api/productos y /api/carrito.

Se deja colección de postman en la carpeta postman

### Run

$ npm start